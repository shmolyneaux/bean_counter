---
description: How to run the BeanBudget Flutter app on Windows
---

1. Ensure Flutter is on your PATH:
```powershell
$env:Path = "Z:\flutter\bin;" + $env:Path
```

// turbo
2. Run the app in debug mode:
```powershell
flutter run -d windows
```
